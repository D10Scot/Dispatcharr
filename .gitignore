.DS_Store
**/__pycache__/
**/.vscode/
**/venv
*.pyc
node_modules/
.history/
staticfiles/
docker/DockerfileAIO
docker/Dockerfile DEV
static/
data/
.next
next-env.d.ts
media/
celerybeat-schedule*
dump.rdb
debugpy*
uwsgi.sock
.uwsgi-reload
package-lock.json
!e2e/package-lock.json
!e2e-upstream/package-lock.json
models
.idea
.vite/
playwright-report/
test-results/

# E2E auth state — contains live JWTs
e2e/playwright/.auth/
e2e-upstream/dist/
# Built by scripts/make-asset.sh, normally only inside the Docker builder
# stage; .dockerignore already keeps it out of the build context, but it
# also needs to stay untracked for anyone who runs the script on the host.
e2e-upstream/assets/

# brainstorming visual-companion artefacts (superpowers plugin)
.superpowers/
